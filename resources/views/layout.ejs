<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Real Time Pizza</title>
    <!-- <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> -->
    <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,400;0,700;1,300&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    
    <!-- <h1>Hello from Home Page....</h1> -->


    <nav class="container mx-auto flex items-center justify-between py-4 px-3">
        <div>
            <img src="/img/logo.png" alt="logo">
        </div>
        <div>
            <ul class="flex items-center">
                <li class="ml-6"><a href="#">Menu</a>
                <li class="ml-6"><a href="#">Offers</a>
                <% if(user) { %>
                    <li class="ml-6">
                        <form id="logout" action="/logout" method="POST">
                        <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                        </form>
                    </li>
                <% } else { %>
                    <li class="ml-6"><a href="/register">Register</a>
                    <li class="ml-6"><a href="/login">Login</a>
                <% } %>
                <li class="ml-6"><a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
                    
                    <span id="cartCounter" class="text-white font-bold pr-3"><%= session.cart ? session.cart.totalQty : '' %></span>

                    <img src="/img/cart.png" alt="cart logo"></a></li>
            </ul>
        </div>
    </nav>

    <%- body -%>

    <footer></footer>

    <script src="/js/app.js">

    </script>
</body>
</html>